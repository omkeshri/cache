Network Tab -> Any interaction that happens between browser and request that goes beyond the browser.

```
Device --> Router --> ISP --> DNS --> Device(same route return) --> Router --> ISP --> Server --> Page Rendered
(domain)                                  (got ip address)   
